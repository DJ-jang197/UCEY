Place external raw datasets in this directory.

Expected default file for ingestion:

- `federal_contaminated_sites.csv`

The ingestion script can be pointed at another path by setting `BROWNFIELD_CSV_PATH`.
